# Graphite 1.17.0

В списке задач можно быстро найти пункт по тексту, не прокручивая все группы. Обновление прилетит само (`1.17.0 > 1.16.0`).

## Задачи
- Поле «Фильтр задач…» под шапкой. Ищет по тексту чекбокса и названию заметки/плана.
- Совпадения подсвечены; счётчик «N из M». Пустой результат — «Нет совпадений».
- Совпавшее имя группы показывает все её пункты. Esc сбрасывает фильтр.
- Ctrl+Alt+T (и Ctrl+K → «Фильтр задач») открывает раздел и ставит курсор в поле.

Полная история деплоев — в `CHANGELOG.md`.
