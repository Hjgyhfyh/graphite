# Graphite 1.13.0

Граф больше не только «весь vault»: можно смотреть окрестность открытой заметки. Обновление прилетит само (`1.13.0 > 1.12.0`).

## Граф
- Переключатель «Окрестность»: текущая заметка и соседи по `[[ссылкам]]` (один шаг).
- Смена заметки пересобирает окрестность и вписывает её в кадр.
- Нет открытой заметки — подсказка открыть; кнопка «Весь граф» возвращает полный вид.
- Поиск на графе в этом режиме ищет только среди видимых узлов.

Полная история деплоев — в `CHANGELOG.md`.
