# Graphite 1.33.0

На графе в режиме окрестности клик по соседу больше не выкидывает в дерево — окрестность переезжает вместе с заметкой. Ctrl+клик копирует `[[вики-ссылку]]`. Обновление прилетит само (`1.33.0 > 1.32.0`).
