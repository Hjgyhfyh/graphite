# Graphite 1.2.0

Навигация по дню и окну поверх 1.1.0. Обновление прилетит само (`1.2.0 > 1.1.0`).

## Окно
- Заголовок native-окна и вкладки браузера: открытая заметка или имя раздела. В режиме фокуса — префикс «Фокус ·». Отдельные окна захвата и заметки не трогаем.

## Дневник
- Ctrl+Shift+J открывает сегодняшний день (календарь тоже перескакивает на текущий месяц).
- Та же команда в Ctrl+K («Сегодня в дневнике») и по клику на дату рядом с именем хранилища в статусбаре.

## Теги
- Облако тегов берёт цвета из токенов темы (`--accent`, `--ai`, `--ok` …), а не из неоновой палитры под тёмный фон. На «Бумаге» чипы остаются бумажными.

Полная история деплоев — в `CHANGELOG.md`.
