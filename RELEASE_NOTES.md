# Graphite 1.15.0

Клик по тегу сразу открывает облако с этим тегом — не надо искать его глазами. Обновление прилетит само (`1.15.0 > 1.14.0`).

## Теги
- В свойствах заметки клик по чипу (не по крестику) открывает раздел «Теги» и выбирает этот тег.
- В режиме чтения — чипы в шапке и `#теги` в тексте.
- На канбане клик по тегу на карточке не открывает заметку, а показывает облако.
- В редакторе: Ctrl+клик (⌘+клик) по подсвеченному `#тегу`.

Полная история деплоев — в `CHANGELOG.md`.
