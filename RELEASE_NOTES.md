# Graphite 1.7.0

Заметку можно размножить в один жест. Обновление прилетит само (`1.7.0 > 1.6.0`).

## Дубль
- **Дублировать** в меню дерева и закрепов: новая заметка рядом с исходной, не во «Входящие».
- Имя `Название (копия)` / `(копия 2)`… Тело, тип, теги и иконка копируются, id и даты — новые.
- Ctrl+Alt+D дублирует открытую; в Ctrl+K — «Дублировать заметку».
- Несохранённые правки в редакторе успевают записаться перед копией.

Полная история деплоев — в `CHANGELOG.md`.
