# Graphite 1.12.0

В дереве заметок появился фильтр по имени — не нужно открывать глобальный поиск, чтобы найти файл в длинном списке. Обновление прилетит само (`1.12.0 > 1.11.0`).

## Дерево
- Поле «Фильтр заметок…» под шапкой. Счётчик совпадений, Esc сбрасывает.
- Ctrl+Shift+E (и Ctrl+K → «Фильтр дерева») открывает дерево и ставит курсор в поле.
- Совпадения подсвечены. Совпавшая папка не прячет детей; родители пути остаются видны.
- Нет совпадений — короткое пустое состояние, не пустой экран «создайте первую заметку».

Полная история деплоев — в `CHANGELOG.md`.
