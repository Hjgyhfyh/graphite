# Graphite 1.9.0

Навёл на `[[ссылку]]` — увидел, куда она ведёт, не открывая. Обновление прилетит само (`1.9.0 > 1.8.0`).

## Вики-ссылки
- В редакторе `[[заметка]]` подчёркнута акцентом, скобки приглушены.
- Наведение (~0.4 с) показывает карточку: название и короткий абзац. Нет файла — «Заметка не найдена».
- То же превью в режиме чтения.
- Поддерживается `[[цель|подпись]]`. Внутри кода и YAML не срабатывает.

Полная история деплоев — в `CHANGELOG.md`.
