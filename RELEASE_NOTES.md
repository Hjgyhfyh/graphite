# Graphite 1.11.0

Превью `[[ссылки]]` теперь не только смотреть: клик по карточке открывает заметку. В редакторе — Ctrl+клик по самой ссылке. Обновление прилетит само (`1.11.0 > 1.10.0`).

## Вики-ссылки
- Карточка превью кликабельна (в правке и в режиме чтения), подсказка «Открыть · клик».
- Ctrl+клик (Cmd на macOS) по `[[ссылке]]` в редакторе открывает цель, курсор не прыгает.
- Нет файла — тост «пока не найдена», как при переходе из чтения.

Полная история деплоев — в `CHANGELOG.md`.
